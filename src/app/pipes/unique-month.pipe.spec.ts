import { UniqueMonthPipe } from './unique-month.pipe';

describe('UniqueMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
